#!/bin/sh

cd $HOME/pix/fundal || exit 1

image="$(find . -type f | tofi --prompt-text 'wallpaper ')"

[ -z "$image" ] && exit 1

if pgrep -x swaybg; then
	killall swaybg
	setsid -f swaybg -m fill -i "$image"
	notify-send "$image" "Fundal temporar setat."
fi
